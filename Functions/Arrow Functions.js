const names = ['ted', 'nano', 'nico'];

/*
const test = names.map(function(item){
  return item + " test";
});

console.log(test);
*/

/*
const test = names.map(item => {
  return item + " test";
});

const test2 = names.map((item,index) => {
	console.log("we are on ", index);
  return item + " test";
});
*/

const test = names.map(item => item + " test");

